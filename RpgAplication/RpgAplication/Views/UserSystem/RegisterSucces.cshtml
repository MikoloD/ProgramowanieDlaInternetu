@model RpgAplication.Models.UserModel

@{
    ViewData["Title"] = "Register";
}

<h1>Register</h1>

<h4>UserModel</h4>
<div class="col d-flex justify-content-center">
    <div class="card text-white bg-secondary mb-3 opacity-card" style="width: 18rem;">
        <div class="card-body">
            <p class="card-text text-center">Możesz rozpocząć już swoją przygodę</p>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
